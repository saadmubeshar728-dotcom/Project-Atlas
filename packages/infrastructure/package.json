{
  "name": "@atlas/infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "SQLite database, repositories, and the local IPC receiver for extension imports. Per Architecture Specification section 9, no UI code may import this package directly - go through @atlas/application services.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@atlas/domain": "workspace:*",
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.5.4",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
