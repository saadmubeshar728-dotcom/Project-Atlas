{
  "name": "project-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Project Atlas - lead generation browser extension and desktop app.",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
